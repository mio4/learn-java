```
介绍下所做项目
Spring整合mybatis实现对数据的管理，redis进行token存储，用于后端获取，未考虑失效的问题。主要是别人设计，负责实现，未对redis有深入了解。
java中修饰符的作用域以及可见性？
public ,proctect,private,default。基本正确。
java数据类型分为基本类型和引用类型，基本类型有哪些,引用类型有哪些？
int,char,shot,long,byte,boolean,float,double。 基本正确，但是不全。
什么是值传递和引用传递，包装类型用的是值传递还是引用传递？
值传递传递的是值，引用传递的是指针。包装类型传递的是引用。
Java 中能创建 volatile 数组吗？
可以，volatile基本原理，volitale 保护的地址。
现在有线程 T1、T2 和 T3。你如何确保 T2 线程在 T1 之后执行，并且 T3 线程在 T2 之后执行？
使用join.
既然 start() 方法会调用 run() 方法，为什么我们调用 start() 方法，而不直接调用 run() 方法？
多线程有五种状态，创建->就绪->阻塞->运行->死亡，start创建的线程是就绪状态。未回答为什么不能调用run方法。
volatile和synchronized对比？
Volatile保持可见性和部分原子性。Synchronized调用底层lock锁，资源消耗特别大，是可重入锁。能保持可见性和原子性。Volatile不会造成多线程阻塞。
什么是事务，其有什么特性？
数据库的一系列操作，是数据库的基本操作。具有ACID四个性。描述了四种隔离级别，常用的是第三种。
类型timestamp与datetime有什么区别?
未答出。
类加载器双亲委派模型机制？为什么要使用双亲委派模型？
类加载时，先不进行加载，先调用父类去加载，直到父类无法加载无法加载。未答出为什么需要双亲委派模型。
jvm如何认定两个对象同属于一种类型？
Java对象为存储在堆中，包含了equal方法。可以使用==比较地址，可以使用equals（包含从写）来判读连个对象元素是否相等。 未答出怎么判定是否是是一种类型。
13. 100层楼的问题？
使用二分法测试。未描述具体如何做。
```



```
计算机中怎么表示负数
使用补码方式，对正数二进制每一位取反，然后加1
重载和重写有什么区别
重载是多个同名函数具有不同个数或者类型的参数
重写是子类继承复写，子类方法可以复写父类方法，final修饰的方法不能被重写
final关键字如何使用
可以修饰类 方法和成员变量，修饰类不可被继承，修饰方法不可被重写，修饰成员变量在多线程忠是安全的。用final关键字修饰的类有string等。
大量string字符粗的拼接如何提高性能
使用stringbuilder和线程安全的stringbuffer，减少中间对象的创建
使用过哪些java集合类
对集合的继承关系，List hash和set都比较了解，对其实现方式都比较了解。
6． 怎么保证对象的线程安全
理解为什么会出现线程安全的问题，以及java的内存模型和使用锁的方式保证数据的线程安全。
对加锁的对象也都有理解。
Volatile的作用是什么
禁止指令重排序，及时将线程工作区间的值写回到工作内存。
对于细节和happen-before不太清楚
线程的生命周期
理解线程从创建到终止的阶段，包括线程的等待，阻塞等。
讲一下生产者-消费者模型的实现方式
用队列来存放数据，设定一个两个p v操作，使用mutex表示互斥操作，判断队列是否为空或者为满
有使用过ForkJoin吗
没有
数据库innodb下的聚集索引和非聚集索引的区别
对索引的概念基本了解。聚集索引的数据放在叶子节点，非聚集索引叶子节点存放主键值，会产生两次查找
Mysql的事务隔离性
知道隔离级别，了解基本的场景。
有用过什么设计模式
单例模式，观察者模式有了解
N个数中找出第m大的数
使用堆排序实现
三门问题的概率问题
思路错误
求一个正整数的开平方根
用二分法的方式
```



```
spring-boot的作用 不清楚，引用了mybaits，不知道spring-framework
spring如何实现依赖注入的，使用spring的上下文，不知道依赖注入，也不知道如何管理切面的
使用JAVA开发过桌面程序，协同工具算法，对算法的说明不清晰
写线程安全的单例：使用了内部静态类，类的初始化过程，类如何加载？如何初始化了解一些，但是不太清楚如何加载。为什么只有一个？虚拟机保证，加载到常量池，说的不清楚。有哪些线程安全的集合？所写的程序里final的作用？不太清楚。final修饰的类是线程安全的，太错了。HashMap的原理？不太清楚。
求数组中两个值为给定值。说思路时是先排序。不对。提示后用hash表存，写JAVA不熟悉，写的很慢，基础语法也有问题。
sql的where和on有什么区别，left join与right join有什么区别：
问了下两个获奖项目的原理，思路等，不是很清楚项目的内容，可能是打酱油的。对其中电商网站的web项目较熟悉，自己写的部分在1000行左右，但是对中间的业务逻辑说不清楚。
使用git, git如何创建分支？git branch 用git merge用来合并
博客地址 https://blog.csdn.net/softwarex4?t=1 算法多，leetcode上的算法答案，但是在说最熟悉的一个算法思路时不清晰。
```



```
介绍下所做项目
西电宿舍项目，作为组要负责人在管理项目，团队自己搭建 CoAP、LWM2M、TUP 服务器，适配移远 BC95-B8、BC35 等芯片。实现利用移动 NB 卡，在数据不经过运营商平台的情况下，直接发送到自己的 Microthings 平台。清晰描述自己工作和考虑问题的思路以及解决方式。
java数据类型分为基本类型和引用类型，基本类型有哪些,引用类型有哪些？
Int,long,char,double,float,Boolean,short，byte。以及对应的包装类，未回答引用类型有哪些。
什么是基本类型的包装类型，基本类型与包装类型的异同？
主要差异包装类型在堆，基本类型在栈。其他就是在是使用上的差别。
java程序初始化的顺序是什么样子的？
父类先加载，子类后加载。
父类静态代码块->子类静态代码块…（基本正确）
Try catch finally的问题
try先执行，finally一定执行。（感觉描述有点问题，未细考察，但基本正确）
什么是原子操作。Double读取是原子操作吗？
最小执行单位，要么执行，要么不执行。Double读取不是原子操作，但未描述正确为什么不是原子操作，
volatile关键字在Java中有什么作用
保证可见性和顺序性。
volatile vs synchronized?
Synchronized 能保证可见性和原子性，但是不能保证重排。Volatile能保证可见性防止指令重排。
什么是事务，事务的一般特性有哪些？
事务是一系类数据库操作，是数据库操作的基本逻辑单位。具有ACID特性。
类型char和varchar有什么区别？分别适用于哪些场景？
Char 固定长度，varchar不固定长度。适用场景未详细描述，项目中常使用varchar.
四次挥手？
精简正确的描述了四次挥手。正确。
TCP协议如何来保证传输的可靠性?
滑动窗口+超时重传。
类加载器双亲委派模型机制？为什么要使用双亲委派模型？
将加载请求委托给父类。直至不能加载；维护jvm安全，保证java类型体系中最基础的行为。正确。
jvm如何认定两个对象同属于一个类型？
同一个类型生成，同一个加载器加载。正确。
简述java垃圾回收机制？
回答了新生代，老年代，已经常见的垃圾回收算法。正确。
高性能与高并发？
项目可扩展，nginx动态添加服务器，mysql建立有主从机制。使用多种框架，使用分布式缓存。
100层楼的问题？
a)使用重力来计算。
b)二分法，估算大致范围，但未描述如何做。未能有效解答。
网络无问题情况下，服务出现短时间内不可用，该如何分析？如何排查？
分析：内存问题。线程等待资 
方式：1、查看cpu,内存。2、查看swap是否频繁交换。3、查看是否线程等待资源。

```



```
写代码：线程安全的单例，能写出双重检查的线程安全单例了，但是对voltiale关键字的原因不清，对指令重排也说不清楚。增加难度考虑对象新建后需要初始化的情况，锁加到方法上，分析了缺点；在构造器里调用init(); 代码风格良好。
写代码：开平方根  需要提示才能写完，代码风格良好。
Java程序运行比较慢的情况下，如何分析？Jstack看fullgc，jps看进程，dump看等等。原因是对象未回回收。CPU负载高，线程上下文切换、死循环等。平时的项目中没有相关的定位经验。
Spring的核心思想，IOC依赖注入，管理对象，尤其是大型工程。spring依赖注入的原理。spring如何管理bean，使用多种方式配置后再context里管理。
mybaits 用的不多，熟悉基本的关联查询
ActiveMQ在项目中的应用，用来推送消息，门锁激活后到队列取，消息有过期时间。
部署优化的点，通过写脚本完成zk\nginx\服务\虚机操作等，查看了写的Linux脚本， 掌握Bash脚本的基础语法，能够熟练编写脚本。
项目中研发的系统Microthings系统的架构，包含各个节点的作用，监控，报警
MySQL与MongoDb的区别：结构化的数据与节点数据，时效性。
有自己的技术博客，博客不多，文档写的很少。
是否有同时面试其他部门或公司：百度sre, 字节跳动
```







